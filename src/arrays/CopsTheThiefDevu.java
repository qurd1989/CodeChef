package arrays;
import java.util.*;
public class CopsTheThiefDevu {
    public static void main(String[] args) {
        Scanner sc = new Scanner(System.in);
        int t = sc.nextInt();;
        while (t -- > 0){
            int m = sc.nextInt(); // m is size of array
            int x = sc.nextInt();// x house per min
            int y = sc.nextInt();// y is cop search per min
            int[] arr = new int[m];
            for (int i = 0; i < m; i++){
                arr[i] = sc.nextInt();
            }

        }
    }
}
